services:
  mind-vault:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      # Mount your data directory for persistence
      # Your markdown files go in ./data/
      - ./data:/app/data
    restart: unless-stopped
